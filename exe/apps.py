from django.apps import AppConfig


class ExeConfig(AppConfig):  # ex - ExerciseConfig
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'exe'
    verbose_name = 'Content'    # admin - app name
